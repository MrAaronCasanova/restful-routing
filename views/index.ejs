<h1>INDEX!!!</h1>

<% blogs.forEach(function (blog) { %>
  <div>
    <h2><%= blog.title %></h2>
    <img src="<%= blog.image %>" alt="Blog Post Image">
    <span><%= blog.created %></span>
    <p><%= blog.body %></p>
  </div>
<% }); %>
